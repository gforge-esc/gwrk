## Code Review: Phase 3 — Task Engine — State, Gates & History — NO-GO

### Results
| Task | Title | Verdict | Notes |
|------|-------|---------|-------|
| T016 | Implement src/commands/tasks.ts | FAIL | [BLOCKER] `tasks generate` is not idempotent; resets all task statuses to 'open', causing loss of progress (as reported in UAT Phase 3). |
| T017 | Implement src/utils/state.ts | PASS | Correct Zod schemas and atomic write logic. |
| T018 | Implement src/utils/parser.ts | PASS | Logic correctly parses plan.md into phases and tasks. |
| T019 | Implement src/utils/gate-gen.ts | FAIL | [BUG] Heuristic for file path resolution incorrectly handles feature-relative files like `plan.md`. [BUG] Only extracts the first backticked segment from "Done When", causing broken gates. |
| T020 | Implement src/utils/history.ts | PASS | JSONL history logging with Zod validation. |
| T021 | Implement test strategy for Phase 3 | FAIL | Verification failed due to broken gate scripts generated by T019 (specifically `+x` error). |

### Lint
- **Status**: CLEAN (Auto-fixed)
- **Note**: Applied `biome check --write` to fix formatting in `src/utils/gate-gen.ts` and `specs/001-cli-core/.gwrk/tasks.json`.

### Tests
- **Status**: PASS
- **Command**: `pnpm test`
- **Results**: 11 files passed, 30 tests passed (All Phase 3 tests: TR-004, TR-006, TR-007).

### Gates
- **Status**: NO-GO
- **Tasks**: T018, T019, T021 failed or had buggy scripts.

### Findings & Blockers
1. **[BLOCKER] Destructive `tasks generate`**: The implementation of the `generate` subcommand in `src/commands/tasks.ts` creates a fresh `TaskState` object and saves it, overwriting any existing progress. It must be updated to load existing state and merge task statuses/timestamps.
2. **[BUG] Gate Path Resolution**: In `src/utils/gate-gen.ts`, the heuristic for identifying files in task descriptions does not prefix feature-relative files (like `plan.md`) with the `featureDir`.
3. **[BUG] "Done When" Regex**: `src/utils/gate-gen.ts` uses `dw.match(/`(.*?)`/)` which only captures the first match. In cases like `Gate files have `+x` permission: `test -x ...``, it only captures `+x`.

### Next Steps
1. Re-open Phase 3 and task T016 in the task engine.
2. Re-open task T019 to fix generator bugs.
3. Run `/implement specs/001-cli-core 3` to remediate findings.

type: workflow
name: gwrk-research-jtbd
version: 0.1.0
description: Execute a Jobs-To-Be-Done (JTBD) research methodology to understand user needs and outcomes.
outputSchema:
  type: object
  properties:
    summary:
      type: string
    intents:
      type: array
      items:
        type: object
        properties:
          action:
            type: string
            enum: [WRITE_FILE, CREATE_DIR, RUN_COMMAND]
          filePath:
            type: string
          content:
            type: string
          dirPath:
            type: string
          command:
            type: string
        required: [action]
  required: [summary, intents]

/**
 * Assembles deep project context for the agent from local files and SQLite (Phase 2).
 */
export async function buildProjectContext(projectRoot: string): Promise<string> {
  throw new Error("Not implemented: buildProjectContext");
}

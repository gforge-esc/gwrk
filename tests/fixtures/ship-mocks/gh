#!/usr/bin/env bash

      if [[ "$1" == "pr" && "$2" == "list" ]]; then
        echo ""
        exit 0
      fi
      if [[ "$1" == "pr" && "$2" == "create" ]]; then
        echo "https://github.com/mock/pr/1234"
        exit 0
      fi
      echo "mock gh"
    
